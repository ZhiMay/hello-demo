- if @user.errors.any?
  #error_explanation
    .alert.alert-danger
      %span The form contains 
      %span=pluralize(@user.errors.count, "error")
      %ul
        -@user.errors.full_messages.each do |msg|
          %li= msg


